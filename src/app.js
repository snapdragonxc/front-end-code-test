import React from 'react';
import ReactDOM from 'react-dom';
import { ShopContainer } from './ShopContainer';

// This component renders the Shop to the HTML.

ReactDOM.render(<ShopContainer />, document.getElementById('root'));
